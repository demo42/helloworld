version: v1.1.0
steps:
  - id: bash
    cmd: |
      orca run demo42t.azurecr.io/base-artifacts/bash
      ls -l
  - id: helm-deploy
    # Once the images are built, and we have credentials
    # coalesce the parallel build steps to do the helm upgrade
    cmd: >
      "orca run demo42t.azurecr.io/base-artifacts/acr/helm helm upgrade helloworld ./charts/helloworld/ --reuse-values --set helloworld.image=$Registry/demo42/helloworld:$ID"
  