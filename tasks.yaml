version: 1.0.0
steps:
  - run: bash ls
  - run: build -t {{.Build.Registry}}/helloworld:release{{.Build.ID}} .
  - run: docker push {{.Build.Registry}}/helloworld:release{{.Build.ID}} .
  - run: {{.Build.Registry}}/functions/helm 
